{
  "name": "hellobahrain-ecommerce",
  "version": "1.0.0",
  "description": "E-commerce application with Next.js web client and React Native mobile app",
  "private": true,
  "workspaces": [
    "client",
    "mobile"
  ],
  "scripts": {
    "dev": "cd client && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:mobile": "cd mobile && npm start",
    "install:all": "npm install && cd client && npm install && cd ../mobile && npm install",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
