# 🍎 iOS Simulator Setup (Mac Only)

## ⚠️ Important: Mac Only!

**iOS Simulator only works on Mac computers.**

If you have Windows:
- ✅ Use Android emulator (see ANDROID_STUDIO_SETUP.md)
- ✅ Use your iPhone with Expo Go (recommended!)

## 📥 Step 1: Install Xcode

1. **Open App Store** on your Mac
2. **Search for "Xcode"**
3. **Click "Get" or "Install"**
4. **Wait for download** (large file, ~10GB, takes 30-60 minutes)
5. **Open Xcode** after installation

## 🔧 Step 2: Install Command Line Tools

1. **Open Terminal**
2. **Run:**
   ```bash
   xcode-select --install
   ```
3. **Click "Install"** in popup
4. **Wait for installation**

## 📱 Step 3: Install iOS Simulator

1. **Open Xcode**
2. **Go to:** Xcode → Preferences → Components
3. **Download iOS Simulator** (choose latest version)
4. **Wait for download**

## ✅ Step 4: Test

1. **Navigate to project:**
   ```bash
   cd ~/Desktop/Helloone.app/mobile
   ```
2. **Start Expo:**
   ```bash
   npm start
   ```
3. **Press 'i' in terminal**
4. **iOS Simulator should open!**

## 🐛 Troubleshooting

### Error: "xcode-select not found"
- Make sure Xcode is installed
- Run: `xcode-select --install`

### Simulator is slow
- This is normal - simulators are slower than real devices
- Use your iPhone instead for better performance!

### Still not working?
- Use your iPhone with Expo Go instead - it's easier and faster!

## 💡 Pro Tip

**Just use your iPhone!** It's:
- ✅ Faster to set up
- ✅ Better performance
- ✅ More realistic testing
- ✅ Works on both Android and iOS

You don't need iOS Simulator unless you specifically need it!








## ⚠️ Important: Mac Only!

**iOS Simulator only works on Mac computers.**

If you have Windows:
- ✅ Use Android emulator (see ANDROID_STUDIO_SETUP.md)
- ✅ Use your iPhone with Expo Go (recommended!)

## 📥 Step 1: Install Xcode

1. **Open App Store** on your Mac
2. **Search for "Xcode"**
3. **Click "Get" or "Install"**
4. **Wait for download** (large file, ~10GB, takes 30-60 minutes)
5. **Open Xcode** after installation

## 🔧 Step 2: Install Command Line Tools

1. **Open Terminal**
2. **Run:**
   ```bash
   xcode-select --install
   ```
3. **Click "Install"** in popup
4. **Wait for installation**

## 📱 Step 3: Install iOS Simulator

1. **Open Xcode**
2. **Go to:** Xcode → Preferences → Components
3. **Download iOS Simulator** (choose latest version)
4. **Wait for download**

## ✅ Step 4: Test

1. **Navigate to project:**
   ```bash
   cd ~/Desktop/Helloone.app/mobile
   ```
2. **Start Expo:**
   ```bash
   npm start
   ```
3. **Press 'i' in terminal**
4. **iOS Simulator should open!**

## 🐛 Troubleshooting

### Error: "xcode-select not found"
- Make sure Xcode is installed
- Run: `xcode-select --install`

### Simulator is slow
- This is normal - simulators are slower than real devices
- Use your iPhone instead for better performance!

### Still not working?
- Use your iPhone with Expo Go instead - it's easier and faster!

## 💡 Pro Tip

**Just use your iPhone!** It's:
- ✅ Faster to set up
- ✅ Better performance
- ✅ More realistic testing
- ✅ Works on both Android and iOS

You don't need iOS Simulator unless you specifically need it!








## ⚠️ Important: Mac Only!

**iOS Simulator only works on Mac computers.**

If you have Windows:
- ✅ Use Android emulator (see ANDROID_STUDIO_SETUP.md)
- ✅ Use your iPhone with Expo Go (recommended!)

## 📥 Step 1: Install Xcode

1. **Open App Store** on your Mac
2. **Search for "Xcode"**
3. **Click "Get" or "Install"**
4. **Wait for download** (large file, ~10GB, takes 30-60 minutes)
5. **Open Xcode** after installation

## 🔧 Step 2: Install Command Line Tools

1. **Open Terminal**
2. **Run:**
   ```bash
   xcode-select --install
   ```
3. **Click "Install"** in popup
4. **Wait for installation**

## 📱 Step 3: Install iOS Simulator

1. **Open Xcode**
2. **Go to:** Xcode → Preferences → Components
3. **Download iOS Simulator** (choose latest version)
4. **Wait for download**

## ✅ Step 4: Test

1. **Navigate to project:**
   ```bash
   cd ~/Desktop/Helloone.app/mobile
   ```
2. **Start Expo:**
   ```bash
   npm start
   ```
3. **Press 'i' in terminal**
4. **iOS Simulator should open!**

## 🐛 Troubleshooting

### Error: "xcode-select not found"
- Make sure Xcode is installed
- Run: `xcode-select --install`

### Simulator is slow
- This is normal - simulators are slower than real devices
- Use your iPhone instead for better performance!

### Still not working?
- Use your iPhone with Expo Go instead - it's easier and faster!

## 💡 Pro Tip

**Just use your iPhone!** It's:
- ✅ Faster to set up
- ✅ Better performance
- ✅ More realistic testing
- ✅ Works on both Android and iOS

You don't need iOS Simulator unless you specifically need it!








## ⚠️ Important: Mac Only!

**iOS Simulator only works on Mac computers.**

If you have Windows:
- ✅ Use Android emulator (see ANDROID_STUDIO_SETUP.md)
- ✅ Use your iPhone with Expo Go (recommended!)

## 📥 Step 1: Install Xcode

1. **Open App Store** on your Mac
2. **Search for "Xcode"**
3. **Click "Get" or "Install"**
4. **Wait for download** (large file, ~10GB, takes 30-60 minutes)
5. **Open Xcode** after installation

## 🔧 Step 2: Install Command Line Tools

1. **Open Terminal**
2. **Run:**
   ```bash
   xcode-select --install
   ```
3. **Click "Install"** in popup
4. **Wait for installation**

## 📱 Step 3: Install iOS Simulator

1. **Open Xcode**
2. **Go to:** Xcode → Preferences → Components
3. **Download iOS Simulator** (choose latest version)
4. **Wait for download**

## ✅ Step 4: Test

1. **Navigate to project:**
   ```bash
   cd ~/Desktop/Helloone.app/mobile
   ```
2. **Start Expo:**
   ```bash
   npm start
   ```
3. **Press 'i' in terminal**
4. **iOS Simulator should open!**

## 🐛 Troubleshooting

### Error: "xcode-select not found"
- Make sure Xcode is installed
- Run: `xcode-select --install`

### Simulator is slow
- This is normal - simulators are slower than real devices
- Use your iPhone instead for better performance!

### Still not working?
- Use your iPhone with Expo Go instead - it's easier and faster!

## 💡 Pro Tip

**Just use your iPhone!** It's:
- ✅ Faster to set up
- ✅ Better performance
- ✅ More realistic testing
- ✅ Works on both Android and iOS

You don't need iOS Simulator unless you specifically need it!









